import { CalendarDays, Mail, ShieldCheck, UserRound } from 'lucide-react';
import { useAuth } from '../context/AuthContext';

export default function Profile() { const { user } = useAuth(); return <div className="profile-page"><div className="page-heading"><div><span className="eyebrow">Account</span><h1>My profile</h1><p>Your identity and privacy settings for this workspace.</p></div></div><section className="panel profile-card"><div className="profile-large-avatar">{user.name.split(' ').map((part) => part[0]).slice(0, 2).join('').toUpperCase()}</div><div className="profile-details"><span className="eyebrow"><ShieldCheck size={14} /> Verified account</span><h2>{user.name}</h2><div><Mail size={15} /> {user.email}</div><div><CalendarDays size={15} /> Member since {new Date(user.created_at).toLocaleDateString(undefined, { month: 'long', year: 'numeric' })}</div></div></section><section className="panel profile-info"><h2>Data privacy</h2><p>Your assessments and reports are linked to this account and are returned only after your JWT is verified.</p><div><ShieldCheck size={17} /> Account-scoped health data</div></section></div>; }
